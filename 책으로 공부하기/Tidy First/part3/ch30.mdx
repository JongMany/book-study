# 콘스탄틴의 등가성

소프트웨어 비용은 그것을 변경하는데 드는 비용과 거의 같음.

- 비용(소프트웨어) ~= 비용(변경)

가장 비용이 많이 드는 하나의 변경이 나머지 변경을 모두 합친 것보다 훨씬 더 많은 비용이 듬.

- 비용(전체 변경) ~= 비용(큰 변경들)

큰 변경의 비용이 비싼 이유는 한 요소의 변경이 다른 여러 요소에 변경을 요구하기 때문.

- 요소의 변경 전파가 발생하는 원인 - 결합도

- 비용(큰 변경들) ~= 결합도

---

비용(소프트웨어) ~= 비옹(전체 변경) ~= 비용(큰 변경들) ~= 결합도
